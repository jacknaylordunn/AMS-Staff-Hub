import React from 'react';

// This file is obsolete. Event management is now part of the Rota page,
// and time clock functionality is in TimeClock.tsx. This is left
// as an empty component to prevent build errors from old imports.
const EventsPage: React.FC = () => {
    return null;
};

export default EventsPage;