import React from 'react';

// This component is obsolete. Event creation is now handled directly
// within the ShiftModal. This is left as an empty component
// to prevent any potential build errors.
const EventModal: React.FC = () => {
    return null;
};

export default EventModal;