import React from 'react';

// This component is obsolete. Repeating events is now handled by
// the RepeatShiftModal. This is left as an empty component
// to prevent any potential build errors.
const RepeatEventModal: React.FC = () => {
    return null;
};

export default RepeatEventModal;